import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { PropsSignup } from '../../typeScript/navType'


const Signup = ({navigation}: PropsSignup) => {
  return (
    <View>
      <Text>Signup</Text>
    </View>
  )
}

export default Signup

const styles = StyleSheet.create({

})